import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.demosecure',
  appName: 'demosecure',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
